spring:
  application:
    name: oj-problem-service
  datasource:
    username: root
    password:
    url: jdbc:mysql://host.ip:3306/online_judge?characterEncoding=UTF-8
server:
  port: 9002
#  servlet:
#    context-path: /user
eureka:
  client:
    service-url:
      defaultZone: http://host.ip:8000/eureka/